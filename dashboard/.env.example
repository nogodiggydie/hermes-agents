# Copy to .env.local and fill in real values
# DO NOT commit the real .env.local

# --- Remote agent service (via SSH tunnel) ---
NEXT_PUBLIC_REMOTE_URL=http://localhost:3001
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# --- Local dashboard ---
PORT=3000